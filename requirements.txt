# requirements.txt — 项目依赖
# 安装命令：pip install -r requirements.txt

# ── 核心依赖 ──────────────────────────────────
anthropic>=0.28.0           # Claude API 客户端
requests>=2.31.0            # HTTP 请求
beautifulsoup4>=4.12.0      # HTML 解析
lxml>=5.1.0                 # BS4 高性能解析器后端
pdfplumber>=0.10.3          # PDF 文本层提取

# ── 反爬增强（可选） ──────────────────────────
fake-useragent>=1.4.0       # 随机 UA 池（备用，config 中已内置静态列表）

# ── Selenium（可选，仅 USE_SELENIUM=True 时需要）──
# selenium>=4.20.0
# webdriver-manager>=4.0.0

# ── 工具库 ───────────────────────────────────
python-dotenv>=1.0.0        # 从 .env 文件加载环境变量（推荐开发时使用）
urllib3>=2.0.0              # 已被 requests 依赖，显式固定版本
